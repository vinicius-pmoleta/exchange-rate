package com.exchangerate.features.exchange.presentation

import com.exchangerate.core.structure.MviRenderer
import com.exchangerate.features.exchange.data.ExchangeState

class ExchangeRenderer : MviRenderer<ExchangeState, ExchangeView> {

    override fun render(state: ExchangeState?, view: ExchangeView) {
    }
}